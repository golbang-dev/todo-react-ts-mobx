import React from 'react';

import { HeaderBox, HeaderWrapper } from './HeaderStyle';

const Header = (): JSX.Element => (
  <HeaderWrapper>
    <HeaderBox>To Do</HeaderBox>
  </HeaderWrapper>
);

export default Header;
